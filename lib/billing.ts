export type PaidPlan="PLUS"|"FAMILY";export function buyLink(plan:PaidPlan){return plan==="PLUS"?process.env.TWOCHECKOUT_PLUS_BUY_LINK:process.env.TWOCHECKOUT_FAMILY_BUY_LINK}export function checkoutUrl(base:string,reference:string,email:string|null){const url=new URL(base);url.searchParams.set("REF",reference);url.searchParams.set("SRC","lifelens_web");if(email)url.searchParams.set("EMAIL",email);return url.toString()}
