"use client";import {useState} from "react";import {Mic,Square} from "lucide-react";
export function VoiceCheckin(){const [recording,setRecording]=useState(false);return <button onClick={()=>setRecording(!recording)} aria-pressed={recording} className={`flex w-full items-center gap-4 border p-4 text-left ${recording?"border-[#a14d3b] bg-[#f0dfda]":"border-black/20 bg-[#faf9f5] hover:border-black/40"}`}><span className="grid h-9 w-9 place-items-center border border-black/20">{recording?<Square size={14}/>:<Mic size={17}/>}</span><span><b className="block text-sm">{recording?"Recording check-in":"Record a check-in"}</b><span className="mt-1 block text-xs text-black/45">{recording?"Select again to stop":"Speak naturally about today"}</span></span></button>}
