import {AppShell} from "@/components/app-shell";import {HealthChat} from "@/components/health-chat";
export default function Ask(){return <AppShell eyebrow="Grounded answers" title="Ask LifeLens" description="Find details across your health history. Every factual answer should point back to the record it came from."><HealthChat/></AppShell>}
