import type {Metadata} from "next";
import Link from "next/link";
import {guides} from "@/lib/guides";

export const metadata:Metadata={title:"Personal Health Record Guides",description:"Practical, source-backed guides for organizing medical records, preparing for doctor visits, maintaining medication lists, and coordinating family health information.",alternates:{canonical:"/guides"},openGraph:{title:"LifeLens personal health record guides",description:"Practical guides for keeping health information organized and ready when it matters.",url:"/guides",type:"website"}};

export default function GuidesPage(){return <main className="min-h-screen bg-[#f4f2ec] text-[#182019]"><header className="border-b border-black/20"><div className="mx-auto flex h-16 max-w-6xl items-center px-5"><Link href="/" className="text-lg font-bold">LifeLens</Link><Link href="/features" className="ml-auto text-sm font-semibold">Features</Link></div></header><div className="mx-auto max-w-6xl px-5 py-16 md:py-24"><p className="text-xs font-semibold uppercase tracking-[.16em] text-black/40">LifeLens guides</p><h1 className="mt-5 max-w-4xl text-5xl font-medium leading-[.95] tracking-[-.06em] md:text-7xl">Practical ways to keep health information organized.</h1><p className="mt-8 max-w-2xl text-lg leading-8 text-black/55">Source-backed guides for maintaining a personal health record, preparing for appointments, and coordinating information with people you trust.</p><div className="mt-16 border-t border-black/20">{guides.map((guide,index)=><article key={guide.slug} className="grid gap-5 border-b border-black/20 py-8 md:grid-cols-[70px_1fr_auto] md:items-start"><span className="font-mono text-xs text-black/35">{String(index+1).padStart(2,"0")}</span><div><p className="text-xs font-semibold uppercase tracking-[.12em] text-black/40">{guide.eyebrow}</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.03em]"><Link href={`/guides/${guide.slug}`}>{guide.title}</Link></h2><p className="mt-3 max-w-2xl text-sm leading-6 text-black/55">{guide.description}</p></div><Link href={`/guides/${guide.slug}`} className="text-sm font-semibold underline underline-offset-4">Read guide</Link></article>)}</div><aside className="mt-14 border border-black/20 bg-[#faf9f5] p-6 text-sm leading-6 text-black/55"><b className="text-black">Information, not medical advice.</b> These guides help readers organize and communicate information. They do not diagnose conditions, recommend treatment, or replace instructions from a qualified health professional.</aside></div></main>}
