import {AppShell} from "@/components/app-shell";import {DocumentLibrary} from "@/components/document-library";
export default function Documents(){return <AppShell eyebrow="Your private archive" title="Health documents" description="Keep lab results, visit notes, scans, and medication records together—organized into one reliable history."><DocumentLibrary/></AppShell>}
