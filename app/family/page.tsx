import {redirect} from "next/navigation";import {AppShell} from "@/components/app-shell";import {FamilyCheckinForm} from "@/components/family-checkin-form";import {createSupabaseServer} from "@/lib/supabase/server";
export default async function Family(){const supabase=await createSupabaseServer();if(!supabase)redirect("/login");const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const [{data:members},{data:checkins}]=await Promise.all([supabase.from("family_members").select("id,user_id,member_user_id,email,status,can_view_timeline,can_contribute").or(`user_id.eq.${user.id},member_user_id.eq.${user.id}`).order("created_at",{ascending:false}),supabase.from("family_checkins").select("id,owner_id,author_id,visit_date,note,mood,walking,appetite").or(`owner_id.eq.${user.id},author_id.eq.${user.id}`).order("visit_date",{ascending:false}).limit(100)]);const owned=members?.filter(x=>x.user_id===user.id)??[],joined=members?.filter(x=>x.member_user_id===user.id&&x.user_id!==user.id)??[];return <AppShell eyebrow="Collaborative journal" title="Family Circle" description="Visits and observations from the people trusted to support your care."><div className="grid gap-6 lg:grid-cols-2"><section className="border border-black/20 bg-[#faf9f5] p-5"><h2 className="font-semibold">Your care circle</h2>{owned.length?owned.map(x=><div key={x.id} className="mt-4 border-t border-black/10 pt-4"><p className="text-sm font-semibold">{x.email}</p><p className="mt-1 text-xs text-black/45">{x.status} · timeline {x.can_view_timeline?"on":"off"} · contributions {x.can_contribute?"on":"off"}</p></div>):<p className="mt-4 text-sm text-black/45">Invite family members from the dashboard.</p>}</section><section className="border border-black/20 bg-[#faf9f5] p-5"><h2 className="font-semibold">Circles you support</h2>{joined.length?joined.map(x=><p key={x.id} className="mt-4 text-sm">Shared care circle · {x.status}</p>):<p className="mt-4 text-sm text-black/45">No shared circles yet.</p>}</section></div>{joined.map(x=><div key={x.id} className="mt-6"><FamilyCheckinForm ownerId={x.user_id} ownerName="your family member"/></div>)}<section className="mt-6 border border-black/20 bg-[#faf9f5]"><h2 className="border-b border-black/15 p-5 font-semibold">Family journal</h2>{checkins?.length?checkins.map(x=><article key={x.id} className="border-b border-black/10 p-5"><p className="text-xs uppercase text-black/40">{new Date(`${x.visit_date}T00:00:00`).toLocaleDateString()} · {x.author_id===user.id?"You":"Family"}</p><p className="mt-3 text-sm">{x.note}</p><p className="mt-2 text-xs text-black/45">Mood: {x.mood??"—"} · Walking: {x.walking??"—"} · Appetite: {x.appetite??"—"}</p></article>):<p className="p-8 text-sm text-black/45">No family check-ins yet.</p>}</section></AppShell>}
